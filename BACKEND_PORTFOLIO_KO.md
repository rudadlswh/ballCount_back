# KBO 실시간 경기 정보 플랫폼 — Backend

> KBO 공식 경기 데이터를 안정적으로 수집·정규화하고, 모바일 앱에 실시간 경기 현황과 맞춤형 알림을 제공하는 백엔드 시스템

## 프로젝트 개요

KBO 경기 일정, 점수, 이닝, 주자 상황, 라인업과 선수 기록은 경기 상태에 따라 빠르게 변하며 외부 데이터 형식에도 영향을 받습니다. 모바일 앱이 외부 원천에 직접 의존하면 데이터 형식 변경이나 일시적인 수집 실패가 곧바로 사용자 장애로 이어질 수 있습니다.

이를 해결하기 위해 외부 데이터 수집부터 정규화, 저장, 실시간 전송, 푸시 알림과 운영까지 담당하는 독립 백엔드를 설계·구현했습니다. 앱은 원천 데이터 대신 일관된 REST API와 SSE 스트림을 사용하며, 서버는 마지막으로 검증된 데이터를 보존해 외부 장애의 영향을 격리합니다.

| 항목 | 내용 |
|---|---|
| 담당 | 백엔드 설계·개발, 데이터 모델링, 테스트, 배포·운영 구성 |
| 기간 | `[프로젝트 기간 입력]` |
| 형태 | `[개인 프로젝트 / 팀 프로젝트 입력]` |
| 핵심 기술 | Java 17, Spring Boot 3.4, Spring Data JPA, JDBC, PostgreSQL, Flyway, SSE, APNs, FCM, Docker, GitHub Actions |
| 검증 규모 | 테스트 594개 전체 통과, 테스트 클래스 67개, Flyway 마이그레이션 37개 |

## 시스템 구성

```text
KBO 공식 데이터
      ↓ 수집
Crawler → Parser → Normalize → Transactional Persist
                                      ↓
                              PostgreSQL / Supabase
                               ↙              ↘
                    REST API · SSE         APNs · FCM
                         ↓                    ↓
                    iOS · Android      알림 · Live Activity
```

수집, 파싱, 도메인, 저장소, 서비스, 스케줄러, API 계층을 분리했습니다. 외부 데이터 형식이 바뀌더라도 Parser와 Crawler를 중심으로 수정 범위를 제한하고, 모바일 앱에는 정규화된 계약만 노출하도록 구성했습니다.

## 핵심 구현 1. 경기 상태 기반 적응형 동기화

### 문제

고정 주기의 전체 수집은 경기가 없는 시간에도 요청과 DB 부하를 만들고, 반대로 경기 중에는 데이터 반영이 늦어집니다. 또한 이전 실행이 끝나기 전에 다음 작업이 시작되면 동일 경기에 대한 중복 저장과 알림 중복 발송이 발생할 수 있습니다.

### 해결

- 경기 없음, 경기 전, 경기 임박, 진행 중, 종료 확인 단계로 상태를 나누고 다음 실행 간격을 동적으로 결정했습니다.
- 기본 운영값 기준으로 유휴 상태는 30분, 경기 시작 30분 전부터는 1분, 라이브 경기는 1초, 경기 종료 확인은 30초 간격으로 동기화하도록 구성했습니다.
- `AtomicBoolean`으로 단일 프로세스 내 중복 실행을 차단하고, PostgreSQL advisory lock 또는 인메모리 lock을 선택할 수 있게 해 날짜·작업 단위의 동시 실행을 제어했습니다.
- 잠금 획득 실패, 외부 프로세스가 보유한 잠금, 장시간 유지된 잠금까지 구분해 운영 로그로 남겼습니다.
- 라이브 동기화 실패 후에도 다음 실행이 중단되지 않도록 재시도 간격으로 후속 작업을 다시 예약했습니다.

### 결과

경기가 없는 시간의 불필요한 수집을 줄이면서도 경기 중에는 초 단위 최신성을 확보했습니다. 동시에 동일 작업의 중복 실행을 막아 저장 데이터와 알림의 일관성을 유지했습니다.

## 핵심 구현 2. 변경 감지와 멱등성을 갖춘 데이터 파이프라인

### 문제

실시간 수집에서는 동일한 응답을 반복해서 받는 경우가 많습니다. 응답마다 스냅샷, 이닝별 점수와 알림을 새로 저장하면 쓰기 부하가 커지고 사용자에게 같은 알림이 여러 번 전달될 수 있습니다. 예정 경기의 점수를 `0:0`으로 저장하면 실제 무득점 상황과 아직 시작하지 않은 상태도 구분할 수 없습니다.

### 해결

- DB 내부 관계에는 UUID를 사용하고, 외부 API에는 `날짜-원정팀-홈팀` 형식의 안정적인 공개 경기 ID를 별도로 제공했습니다.
- 예정 경기 점수는 실제 원천 데이터가 확인되기 전까지 `null`로 유지해 “경기 전”과 실제 `0:0`을 구분했습니다.
- 경기 상세, 이닝별 점수와 선택된 점수 상태를 조합해 SHA-256 해시를 만들고, 직전 `raw_hash`와 같으면 스냅샷 저장·라인스코어 갱신·알림·Live Activity 갱신을 모두 생략했습니다.
- 경기 이벤트에는 고유한 `event_key`와 DB 유니크 제약을 적용해 재시도 상황에서도 동일 이벤트가 중복 발송되지 않게 했습니다.
- DB 커밋이 완료된 뒤에만 SSE와 Live Activity 갱신을 발행해, 사용자에게 전달된 상태와 실제 저장 상태가 어긋나지 않도록 했습니다.
- `crawl_jobs`와 `crawl_failures`에 요청, 파싱, 저장 단계별 결과를 별도 트랜잭션으로 기록해 본 작업 실패 시에도 원인을 추적할 수 있게 했습니다.

### 결과

동일 데이터의 반복 저장과 중복 알림을 차단했고, 외부 데이터의 불완전한 값이 내부 모델의 의미를 훼손하지 않도록 했습니다. 수집 실패 시에도 마지막 정상 데이터가 유지되어 앱의 읽기 API가 외부 장애와 분리됩니다.

## 핵심 구현 3. REST, SSE, 푸시를 연결한 실시간 전달

### 문제

경기 상세 화면은 점수와 이닝 변화를 빠르게 반영해야 하지만 모바일 클라이언트의 짧은 주기 폴링은 배터리와 네트워크를 소모합니다. 푸시 알림은 응원팀, 알림 종류, 방해금지 시간과 운영 환경을 모두 고려해야 하며, Live Activity는 같은 내용의 반복 전송을 피해야 합니다.

### 해결

- 일정, 월간 경기, 스코어보드, 경기 상세, 라인스코어, 박스스코어, 라인업, 팀 순위와 출석 기록을 정규화된 REST API로 제공했습니다.
- 경기별 SSE 구독 레지스트리를 구현하고 스냅샷·상태 변경 이벤트를 전송했습니다. 최초 연결 시 최신 상태를 즉시 전달하고, 10초 heartbeat와 종료 경기의 연결 정리로 장시간 연결을 관리했습니다.
- APNs와 FCM 전송 경로를 분리하고 디바이스의 플랫폼·환경·응원팀·알림 설정·모니터링 경기 조건을 기준으로 발송 대상을 선별했습니다.
- 득점, 역전, 경기 시작·종료, 출루, 취소·중단·재개 등 도메인 이벤트를 감지해 상황별 알림 문구를 생성했습니다.
- Live Activity의 content state를 해시로 비교해 변경이 없으면 전송하지 않고, 볼·스트라이크·아웃처럼 자주 바뀌는 필드는 디바운싱하되 점수와 경기 요약 변경은 즉시 전송했습니다.
- 잘못된 디바이스 토큰은 전송 결과에 따라 비활성화해 반복 실패를 방지했습니다.

### 결과

앱의 반복 폴링 의존도를 낮추면서 경기 변화가 발생한 시점에만 데이터를 전달하도록 구성했습니다. 일반 푸시, Android 푸시와 iOS Live Activity가 하나의 경기 상태 파이프라인을 공유해 채널별 데이터 불일치를 줄였습니다.

## 핵심 구현 4. 운영 실수를 차단하는 배포·보안 설계

### 문제

실시간 수집 서버는 잘못된 환경 설정만으로도 개발 환경에서 운영 DB를 수정하거나, 두 개의 writer가 동시에 실행되어 중복 수집·알림을 만들 수 있습니다. 관리자·디바이스 등록 API와 비밀키 역시 공개 환경에서 보호되어야 합니다.

### 해결

- `reader`와 `writer` 런타임 역할을 분리하고, 동기화 기능이 켜진 경우에만 올바른 프로필·스키마·writer 역할 조합으로 기동되도록 시작 단계에서 검증했습니다.
- 로컬·테스트 환경이 운영 스키마에 연결되거나 운영 환경이 개발 스키마를 사용하는 경우 애플리케이션 기동 자체를 실패시켰습니다.
- 운영 writer는 APNs production 환경, Bundle ID, Team ID, Key ID와 키 경로가 모두 올바르게 설정된 경우에만 시작되도록 구성했습니다.
- 관리자·내부 API를 세션 또는 API Key로 보호하고, 상수 시간 비교, CSP, 클릭재킹 방지, 캐시 방지 헤더를 적용했습니다.
- 디바이스 등록 요청에는 본문 크기 제한과 IP·경로 기준 rate limit을 적용했습니다.
- Flyway로 37개 스키마 변경 이력을 관리하고, JPA는 `ddl-auto=validate`로 설정해 애플리케이션의 임의 스키마 변경을 막았습니다.
- 멀티 스테이지 Docker 이미지, non-root 사용자, read-only secret mount, loopback 포트 바인딩과 health check를 적용했습니다.
- GitHub Actions에서 테스트 통과 후 amd64·arm64 이미지를 GHCR에 게시하고, SSH 배포 후 `/healthz` 응답을 최대 5분간 확인하도록 CI/CD를 구성했습니다.
- Thymeleaf와 HTMX 기반 운영 콘솔에서 경기 상태, 수집 이상, 알림 이력과 애플리케이션 로그를 확인할 수 있게 했습니다.

### 결과

설정 오류를 런타임 장애가 아닌 기동 단계의 명확한 실패로 바꾸고, 테스트부터 컨테이너 배포와 상태 확인까지 일관된 배포 경로를 마련했습니다. 별도 상용 모니터링 도구 없이도 주요 수집·알림 문제를 서버 내부에서 추적할 수 있는 운영 기반을 갖췄습니다.

## API 및 데이터 설계

주요 공개 API는 다음과 같습니다.

- 날짜·월별 경기 일정 및 스코어보드
- 경기 상세, 실시간 상태, 이닝별 점수, 박스스코어와 라인업
- 경기별 SSE 실시간 스트림
- 팀 목록과 순위
- 관람 경기 등록·삭제·조회
- APNs·FCM 디바이스 및 Live Activity 토큰 등록

핵심 데이터는 `teams`, `games`, `game_snapshots`, `line_scores`, `game_events`, `notification_devices`, `notification_events`, `live_activity_tokens`, `crawl_jobs`, `crawl_failures`, `attendance_records`로 나누었습니다. API DTO와 영속 엔티티를 분리해 DB 구조가 모바일 계약에 직접 노출되지 않도록 했습니다.

## 테스트 전략

파서, 스케줄러, 서비스, 저장소, MVC 컨트롤러와 운영 안전장치를 계층별로 검증했습니다.

- 외부 응답의 누락·변형·취소·지연·연장 경기 파싱
- 경기 상태별 폴링 간격과 동시 실행 방지
- 동일 해시 재수집 시 저장·전송 생략
- 득점·출루·경기 상태 전환과 중복 이벤트 방지
- APNs·FCM 대상 필터링, 토큰 무효화와 Live Activity 종료
- 공개 API 응답 계약과 예외 응답
- 개발·운영 DB 스키마 격리 및 writer 기동 조건
- 관리자 인증, 요청 크기 제한과 rate limit
- 적용된 Flyway 마이그레이션 무결성

현재 자동화 테스트 결과는 **594개 통과, 실패 0개, 오류 0개, 스킵 0개**입니다.

## 이 프로젝트에서 보여준 역량

- 외부 데이터의 불안정성을 내부 도메인과 API 계약에서 격리하는 설계
- 실시간성, 비용, 데이터 일관성을 함께 고려한 상태 기반 스케줄링
- 해시, 유니크 키, 트랜잭션 후 발행을 조합한 멱등 처리
- REST·SSE·APNs·FCM·Live Activity를 연결한 이벤트 전달 구조
- 환경 격리, 기동 검증, 마이그레이션과 CI/CD까지 포함한 운영 관점의 개발
- 594개 자동화 테스트로 정상 흐름과 예외 상황을 함께 검증하는 품질 관리

## 이력서용 요약

- Spring Boot와 PostgreSQL 기반 KBO 경기 데이터 수집·정규화 API를 설계하고, 경기 상태에 따라 30분~1초로 주기를 조절하는 적응형 동기화 스케줄러를 구현했습니다.
- 원천 응답 SHA-256 변경 감지, 이벤트 고유 키, PostgreSQL advisory lock과 트랜잭션 후 발행을 적용해 중복 저장·알림을 방지하고 데이터 일관성을 확보했습니다.
- REST API와 SSE 실시간 스트림, APNs·FCM 알림 및 iOS Live Activity를 통합했으며, Flyway 37개 마이그레이션과 자동화 테스트 594개로 안정성을 검증했습니다.
- Docker·GitHub Actions 기반 CI/CD, 운영/개발 DB 격리, reader/writer 기동 가드와 관리자 콘솔을 구축해 배포와 운영까지 담당했습니다.

## 면접에서 강조할 한 문장

“빠르게 변하는 경기 데이터를 단순히 자주 가져오는 데 그치지 않고, 상태 기반 수집·멱등 저장·트랜잭션 후 실시간 전송·운영 안전장치를 하나의 파이프라인으로 설계한 프로젝트입니다.”
